SIGNALS INTELLIGENCE PACKAGE // OPERATION IRON VEIL
---------------------------------------------------
Source: Captured forward listening post, Eastern Front
Status: CRITICAL - ACTIVE WARZONE
Handling: COUNTER-INTELLIGENCE, EYES ONLY

BACKGROUND
----------
During the night raid on the enemy listening post we pulled a full dump of
their signal traffic, a field operative registry, a captured grid reference
map, and one encrypted order block. The enemy "PHANTOM CELL" is about to
detonate a device in one of our northern port cities.

We know three things from prisoner interrogation:
  1. The enemy net (callsign GHOST_NET) chatters inside the noise of the
     normal signal traffic. Their bursts are buried in intercepts/signals.log.
  2. The real strike order sits in crypto/cipher_block.txt. It is enciphered
     with their field cipher: a polyalphabetic, repeating-key scheme
     (a classic Vigenere). The key is NOT written down - it is the callsign
     of one specific operator.
  3. The decoded order names the cell LEADER (a codename), a GRID reference,
     and a STRIKE TIME. The grid must be resolved to a city using the
     captured map in maps/grid_reference.csv.

OBJECTIVE
---------
Report the cell LEADER codename, the TARGET CITY, and the STRIKE TIME.

FORMAT
------
The solution is <CODENAME>-<City>-<HHMM>
(example: FALCON-Hamburg-2350)

VERIFICATION
------------
MD5 Hash: e10134e1b1804b86af0afdbeb0cadea4
(echo -n "CODENAME-City-HHMM" | md5)
