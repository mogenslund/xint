# Validation

Successful assessments correspond to a file named `<md5>.success` in this
directory. Calculate the hash exactly as described in
`reference/assessment-format.md`.

The hash validates one intentionally selected five-item evidence package. Other
reasonable evidence may support the conclusion, but this small example uses one
canonical package so validation remains deterministic.
